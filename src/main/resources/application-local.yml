spring:
  datasource:
    url: jdbc:mysql://localhost:3306/societyfest
    username: ramakant
    password: Ramakant@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

#logging:
#  level:
#    org.hibernate.SQL: DEBUG        # Logs SQL statements
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE  # Logs parameters
#    org.hibernate.orm.jdbc.bind: TRACE # For Hibernate 6 (newer versions)

  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: gachinikhil97@gmail.com
    password: gwtz uwgo txlk fqul
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8090

jwt:
  secret-file: /Users/ramakant/Documents/ramakant/project_env_files/secret_local.txt
  expirationMs: 86400000

app:
  frontend-url: http://localhost:5173
