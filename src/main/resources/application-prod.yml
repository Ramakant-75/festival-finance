spring:
  datasource:
    url: jdbc:mysql://localhost:3306/societyfest_prod
    username: produser
    password: India@7506
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # validate schema but do not update
    properties:
      hibernate:
        format_sql: false
server:
  port: 8090

jwt:
  secret-file: /Users/ramakant/Documents/ramakant/project_env_files/secret_prod.txt
  expirationMs: 86400000